[project]
name = "why-do-prices-rise"
version = "0.1.0"
description = "Geopolitical price tracker for fuel and commodities"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.104.0",
  "uvicorn>=0.24.0",
]
